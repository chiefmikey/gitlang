# GitLang Context — 2026-03-01

## Current State

Version 5.0.0. All dependencies updated to latest compatible versions. Production build succeeds, ESLint passes clean. 14 open GitHub issues addressed.

## Recent Changes (v5.0.0)

### Dependency Modernization
- Cleaned unused deps (d3-interpolate, path, url-loader, @types/dotenv-webpack)
- Moved nodemon to devDependencies
- Added missing prettier and stylelint to devDependencies
- Updated Svelte 5.38→5.53, Webpack 5.101→5.105, TypeScript 5.9→5.10
- Removed unused jsx/preact config from tsconfig.json
- Added local ESLint/Prettier/Stylelint configs (replacing @mikey-pro shared packages)

### Bug Fixes
- **#104**: Fixed "Loading..." text staying on screen when user not found
- **#88**: Fixed scroll only working within narrow wrapper div
- **#259**: SonarCloud no longer runs on closed PRs
- **#237, #230**: Deploy workflows use concurrency groups to prevent conflicts

### New Features
- **#219, #92**: API rate limit validation — pre-flight check before fetching, 429 response when insufficient
- **Multi-token rotation**: Support multiple GitHub PATs (comma-separated GH_PAT), round-robin with rate-limit-aware switching
- **#89**: Include forks toggle (checkbox next to submit)
- **#103**: Multiple repos input (username/repo1,repo2,repo3)
- **#101**: Multiple usernames (user1+user2 or space-separated)
- **#105**: Random user button
- **#325**: Clickable language names showing per-repo breakdown

## Open Issues (Not Addressed)

- **#102**: Compile or compare (complex query syntax with + and ? operators) — deferred, complex
- **#203**: Commit level results (linguist integration) — deferred, requires significant API work

## Architecture Notes

- Token management is in `server/helpers/github/tokenManager.ts`
- Per-repo language data is preserved through the data pipeline for breakdown feature
- Input parsing handles org prefixes (@, org:, org/), comma-separated repos, and + separated users
- The wrapper div is now full viewport width with flex centering for scroll-anywhere support
