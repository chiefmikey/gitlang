name: Beta Branch

on:
  create

jobs:
  build:
    name: Branch
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '16.x'

      - name: Checkout
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.DISPATCH_REPO }}
          fetch-depth: 0

      - name: Open PR
        env:
          GH_TOKEN: ${{ secrets.DISPATCH_REPO }}
        run: |
          BRANCH=${GITHUB_REF##*/}
          git config user.name "Mikl Wolfe"
          git config user.email "wolfemikl@gmail.com"
          git checkout main
          git reset --soft ${BRANCH}
          git add .
          git commit -am 'Beta branch'
          gh pr create --title ${BRANCH} --body 'Beta branch' --base 'main' --head main
